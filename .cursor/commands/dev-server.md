---
description: 🌟 Lance le serveur MCP dev avec hot-reload
---

Lance le serveur de développement MCP avec hot-reload.

**Workflow recommandé** : Lancer d'abord `tunnel-only` dans un terminal séparé, puis cette commande.

```bash
cd $WORKSPACE && npm run dev
```

