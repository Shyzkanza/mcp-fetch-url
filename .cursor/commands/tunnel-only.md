---
description: 🌟 Lance ngrok (à laisser tourner pendant le dev)
---

Lance un tunnel ngrok sur le port 3000.

**Workflow recommandé** : Lancer cette commande une fois et la laisser tourner. L'URL ngrok reste la même !

```bash
cd $WORKSPACE && npm run tunnel
```

