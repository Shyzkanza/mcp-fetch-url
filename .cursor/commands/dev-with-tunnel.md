---
description: Lance dev + ngrok ensemble (alternative)
---

Lance le serveur de développement ET ngrok en parallèle.

⚠️ Relance ngrok à chaque fois = nouvelle URL.

```bash
cd $WORKSPACE && npm run dev:tunnel
```

